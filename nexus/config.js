// Nexus 2.0 config.
// Leave both blank = LOCAL mode (data stays in this browser; demo PIN login).
// Fill both = CLOUD mode (Supabase Auth login + data synced for every user). Run supabase-schema.sql first.
window.NEXUS_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
